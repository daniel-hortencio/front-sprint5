import React from 'react';
import { WebsiteTemplate } from '../../templates/Website';

const Home: React.FC = () => (
  <WebsiteTemplate>
    <h1>Página não encontrada</h1>
  </WebsiteTemplate>
);

export default Home;
